<%= form_for(@richrd) do |f| %>
  <% if @richrd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@richrd.errors.count, "error") %> prohibited this richrd from being saved:</h2>

      <ul>
      <% @richrd.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
